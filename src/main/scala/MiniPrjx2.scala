import java.io.{File, FileInputStream, FileOutputStream}

import scala.io.Source
object MiniPrjx2 extends App
{
    ///Users/lakshmi/IdeaProjects/ProjectEuler/Inputs/demo.txt

   /* FileInputStream inputStream
    FileOutputStream outputStream

    val batchSize : scala.io.StdIn.readLine("Enter the Batch Size(Bytes) : ")
    //val filePath : scala.io.StdIn.readLine("Enter the filePath : pwd")
    val filePath : String = "Users/lakshmi/IdeaProjects/ProjectEuler/Inputs/demo.txt"
    try
        {
            File inputFile : File =  new File(filePath)
            inputStream = new FileInputStream(inputFile)
            val bytes = filePath.getBytes(scala.io.Codec.UTF8.charSet)
            while(batchSize > 0)
                {
                   // val bytesChunkPart :
                }
        }
    val bufferSource = Source.fromFile(CurrentDirectory+"/Inputs/demo.txt").getLines()
    for((line,index)<-bufferSource.zipWithIndex)
    {
      println(s"Line no ${index+1} is $line")
    }*/

}
